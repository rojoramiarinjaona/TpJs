//nombre d'apparition d'un element dans une liste
function apparition(element){
    let liste=[];
    let nombre=0;
    for (let i=0;i<liste.length;i++){
        if(liste[i]===element){
        nombre++;
    }
}
    return nombre;
}